## Benchmark Compilation Boilerplate

* [`Makefile`](./Makefile) automatically compiles the list of sources specified using both CuPBoP-AMD and HIPIFY.
* [`run.sh`](./run.sh) compiles and runs the benchmark.
